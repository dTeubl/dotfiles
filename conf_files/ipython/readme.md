
## Notes

**autorealod.ipy** should be placed under **~/.ipython/<profile>/startup/**. It will enable reloading already loaded module during runtime of the kernel. In other words, it grats fast feedback cycles during development, withouth restarting the ipython kernel.

**ipython_config.py** minimalistic configuration file for the live iterperpreter.
